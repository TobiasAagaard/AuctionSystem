# AuctionSystem


## Start Data base

```
// first time setup
docker compose up -d --build

// use afterwards
docker compose up -d
```

## Close Data base

```
docker compose down

// if you want to delete the data as well
docker compose down -v
```